# Fjern upstream branch
git remote rm upstream
# Legg til upstream til fagstab-repoet på alle gitpod instanser som blir kjørt av studenter
git remote add upstream git://github.com/jorgensta/gitpod-test.git